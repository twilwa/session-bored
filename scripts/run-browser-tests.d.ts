// ABOUTME: Describes the browser-run failure classifier consumed by the unit suite.
// ABOUTME: Keeps the executable JavaScript entry point type-safe for TypeScript importers.
export function isWebServerFailure(output: string): boolean;
